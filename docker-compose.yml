version: '3.9'
services:
  app:
    image: node:20
    working_dir: /usr/src/app
    volumes:
      - .:/usr/src/app
    environment:
      - PORT=3000
    command: sh -c "npm install && npm run build && npm run start"
    ports:
      - '3000:3000'
